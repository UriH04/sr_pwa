def simular_movimiento(ruta):
    # Placeholder: Genera posiciones interpoladas (x,y) para animación.
    posiciones = []
    for i, nodo in enumerate(ruta):
        posiciones.append({"x": i * 10, "y": i * 10, "tiempo": i * 10})  # Mock
    return posiciones